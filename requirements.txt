# pip convenience installer. The canonical dependency declaration lives in
# pyproject.toml; this file mirrors it (pinned) plus dev tooling, and installs
# the package itself via `-e .` so `from frag...` imports resolve.
#
#   pip install -r requirements.txt
#
# uv users can instead run `uv sync --extra dev`.

# runtime
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pandas==2.2.2
numpy==2.0.1
scikit-learn==1.5.1
rank-bm25==0.2.2
requests==2.32.3
httpx==0.27.0
python-dotenv==1.0.1
pyyaml==6.0.2
beautifulsoup4==4.12.3
lxml==5.3.0
qdrant-client==1.11.3
sentence-transformers==3.1.1

# dev
pytest==8.3.2
pytest-cov==5.0.0
ruff==0.6.9

# eval
mlflow==3.15.1

# the package itself (editable), so tests import `frag` without a path hack
-e .
